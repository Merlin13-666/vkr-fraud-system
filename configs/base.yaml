seed: 42

schema_path: "configs/schema_ieee_cis.yaml"

data:
  raw_path: "data/raw/ieee-cis"
  processed_path: "data/processed"

split:
  train_ratio: 0.6
  val_ratio: 0.2
  test_ratio: 0.2

target:
  name: "isFraud"